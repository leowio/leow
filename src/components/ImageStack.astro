---
import Image from "astro/components/Image.astro";

export interface Props {
  images: string[];
  alt?: string;
  className?: string;
  imageClassName?: string;
  title?: string;
}

const {
  images,
  alt = "Image stack",
  className = "",
  imageClassName = "",
  title,
} = Astro.props;
---

<div class="flex flex-col items-center">
  <div class={`flex w-full max-w-screen-md ${className}`}>
    {
      images.map((image, index) => (
        <div class="min-w-0 overflow-hidden transition-all hover:shrink-0">
          <Image
            src={image}
            alt={`${alt} ${index + 1}`}
            width={456}
            height={256}
            loading="eager"
            format="webp"
            class={`h-auto min-w-[456px] ${imageClassName}`}
          />
        </div>
      ))
    }
  </div>
  {title && <span class="text-sm font-light italic">{title}</span>}
</div>
